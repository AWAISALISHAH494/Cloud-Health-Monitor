apiVersion: v1
kind: Service
metadata:
  name: cloud-service-monitor-service
spec:
  type: NodePort
  selector:
    app: cloud-service-monitor
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080